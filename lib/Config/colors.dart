import 'package:flutter/material.dart';

// ===========================================================================
// NEW SOILO PALETTE (Glassmorphism & Deep Nature Tones)
// ===========================================================================

// --- DARK THEME (Deep Atmosphere) ---
const dBackgroundColor = Color(0xFF101418); // Deep Space/Black
const dSurfaceColor = Color(0xFF161C22); // Slightly elevated for dark surfaces
const dOrbColor1 = Color(0xFF00695C); // Primary Light (Deep Teal Glow)
const dOrbColor2 = Colors.yellow; // Accent (Warm Amber Glow)
const dPrimaryColor = Color(0xFF00695C); // Primary Light
const dTextColor = Color(0xFFE1E3E4); // Crisp Off-White
const dSubTextColor = Color(0xFFA0AAB2); // Muted slate

// --- LIGHT THEME (Soft Nature) ---
const lBackgroundColor = Color(0xFFF0F4F8); // Soft Blue-Grey
const lSurfaceColor = Color(0xFFF0F4F8); // Matches BG for pure Neomorphism
const lOrbColor1 = Color(0xFFE0F2F1); // Very Light Teal Wash
const lOrbColor2 = Color(0xFFFFCC80); // Light Accent (Soft Peach/Amber Wash)
const lPrimaryColor = Color(0xFF005A50); // Primary Deep Teal
const lTextColor = Color(0xFF1A1C1E); // Deep Ink Text
const lSubTextColor = Color(0xFF5E6A75); // Muted grey